# Database Assets

Keep database reference scripts and exported SQL dumps here.

Use this folder for manual import files, for example:

```txt
database/HomestayBooking.sql
```

If the project later uses Flyway or Liquibase, move versioned migration scripts to
`src/main/resources/db/migration`.
